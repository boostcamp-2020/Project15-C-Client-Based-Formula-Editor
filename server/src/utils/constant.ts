export enum STATUS_CODE {
  SUCCESS = 200,
  CREATED = 201,
  CLIENT_ERROR = 400,
  SERVER_ERROR = 500,
}

export enum ERROR_MESSAGE {
  CLIENT_ERROR = 'Request Error',
  SERVER_ERROR = 'Server Error',
}
